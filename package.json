{
  "name": "aftermark",
  "version": "0.1.0",
  "description": "Aftermark — your bookmarks are breadcrumbs.",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.39",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "idb": "^8.0.0"
  }
}
